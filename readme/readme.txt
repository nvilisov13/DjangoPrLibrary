JavaScript Library -> Dygraph Link: https://dygraphs.com/
Список зависимостей
pip install -r .\readme\requirements.txt

Django Admin: "login: admin; password: admin"
User: "login: user; password: user1234"

Обновление пакетного менеджера pip
python -m pip install --upgrade pip

python.exe .\manage.py shell
from library.models import *
from django.db import connection

pip install ipython и ещё один
pip install django-extensions улучшенный интерпретатор Python для запросов Django ORM
python.exe .\manage.py shell_plus --print-sql -возможность видеть SQL запросы выполняемые в Django ORM
